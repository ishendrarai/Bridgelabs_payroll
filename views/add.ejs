<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>EPS — Add Employee</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
<div class="blob-1"></div>
<div class="blob-2"></div>

<aside class="sidebar">
  <div class="sidebar-brand">
    <div class="brand-icon">💼</div>
    <div class="brand-text">
      <span class="brand-name">Payroll</span>
      <span class="brand-sub">EPS v1.0</span>
    </div>
  </div>
  <nav class="sidebar-nav">
    <span class="nav-label">Menu</span>
    <a href="/" class="nav-item"><span class="nav-icon">⬛</span> Dashboard</a>
    <a href="/add" class="nav-item active"><span class="nav-icon">＋</span> Add Employee</a>
  </nav>
  <div class="sidebar-footer">
    <div class="sidebar-badge">
      <strong>GLA University</strong>
      Node.js · Express · EJS
    </div>
  </div>
</aside>

<div class="main">
  <div class="form-wrapper">
    <div class="form-card">
      <div class="form-head">
        <h2>Add New Employee</h2>
        <p>Fill in the details to register a new payroll record</p>
      </div>
      <div class="form-body">
        <% if (error) { %>
          <div class="alert-error">⚠️ <%= error %></div>
        <% } %>
        <form action="/add" method="POST" autocomplete="off">
          <div class="form-group">
            <label>Full Name</label>
            <div class="input-wrap">
              <span class="input-prefix">👤</span>
              <input type="text" name="name" placeholder="e.g. Ravi Sharma" required/>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Gender</label>
              <select name="gender" required>
                <option value="">— Select —</option>
                <option value="Male">Male</option>
                <option value="Female">Female</option>
                <option value="Other">Other</option>
              </select>
            </div>
            <div class="form-group">
              <label>Start Date</label>
              <input type="date" name="startDate" required/>
            </div>
          </div>
          <div class="form-group">
            <label>Department</label>
            <div class="input-wrap">
              <span class="input-prefix">🏛️</span>
              <input type="text" name="department" placeholder="e.g. Engineering" required/>
            </div>
          </div>
          <div class="form-group">
            <label>Basic Salary (₹)</label>
            <div class="input-wrap">
              <span class="input-prefix">₹</span>
              <input type="number" name="salary" placeholder="e.g. 55000" min="0" step="0.01" required/>
            </div>
          </div>
          <div class="form-actions">
            <a href="/" class="btn-cancel">Cancel</a>
            <button type="submit" class="btn-submit">Add Employee</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
</body>
</html>
