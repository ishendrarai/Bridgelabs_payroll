<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title>EPS — Edit Employee</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
<div class="blob-1"></div>
<div class="blob-2"></div>

<aside class="sidebar">
  <div class="sidebar-brand">
    <div class="brand-icon">💼</div>
    <div class="brand-text">
      <span class="brand-name">Payroll</span>
      <span class="brand-sub">EPS v1.0</span>
    </div>
  </div>
  <nav class="sidebar-nav">
    <span class="nav-label">Menu</span>
    <a href="/" class="nav-item"><span class="nav-icon">⬛</span> Dashboard</a>
    <a href="/add" class="nav-item"><span class="nav-icon">＋</span> Add Employee</a>
  </nav>
  <div class="sidebar-footer">
    <div class="sidebar-badge">
      <strong>GLA University</strong>
      Node.js · Express · EJS
    </div>
  </div>
</aside>

<div class="main">
  <div class="form-wrapper">
    <div class="form-card">
      <div class="form-head">
        <h2>Edit Employee</h2>
        <p>Update the payroll record for <strong style="color:var(--text)"><%= employee.name %></strong></p>
      </div>
      <div class="form-body">
        <% if (error) { %>
          <div class="alert-error">⚠️ <%= error %></div>
        <% } %>
        <form action="/edit/<%= employee.id %>" method="POST">
          <div class="form-group">
            <label>Full Name</label>
            <div class="input-wrap">
              <span class="input-prefix">👤</span>
              <input type="text" name="name" value="<%= employee.name %>" required/>
            </div>
          </div>
          <div class="form-row">
            <div class="form-group">
              <label>Gender</label>
              <select name="gender" required>
                <option value="Male"   <%=employee.gender==='Male'  ?'selected':''%>>Male</option>
                <option value="Female" <%=employee.gender==='Female'?'selected':''%>>Female</option>
                <option value="Other"  <%=employee.gender==='Other' ?'selected':''%>>Other</option>
              </select>
            </div>
            <div class="form-group">
              <label>Start Date</label>
              <input type="date" name="startDate" value="<%= employee.startDate %>" required/>
            </div>
          </div>
          <div class="form-group">
            <label>Department</label>
            <div class="input-wrap">
              <span class="input-prefix">🏛️</span>
              <input type="text" name="department" value="<%= employee.department %>" required/>
            </div>
          </div>
          <div class="form-group">
            <label>Basic Salary (₹)</label>
            <div class="input-wrap">
              <span class="input-prefix">₹</span>
              <input type="number" name="salary" value="<%= employee.salary %>" min="0" step="0.01" required/>
            </div>
          </div>
          <div class="form-actions">
            <a href="/" class="btn-cancel">Cancel</a>
            <button type="submit" class="btn-submit">Save Changes</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
</body>
</html>
